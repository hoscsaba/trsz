function out = subsref(fojtas,index)

switch index.type
    case '.'
        out=subsref(fojtas.tranziens_agelem_2csp,index);        
    otherwise
        error('HIBA!!! mezohivatkozas .-al (pl. elem.ro)')
end