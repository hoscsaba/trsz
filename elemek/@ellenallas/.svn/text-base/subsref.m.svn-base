function out = subsref(ellenallas,index)

switch index.type
    case '.'
        out=subsref(ellenallas.tranziens_agelem_2csp,index);        
    otherwise
        error('HIBA!!! mezohivatkozas .-al (pl. elem.ro)')
end